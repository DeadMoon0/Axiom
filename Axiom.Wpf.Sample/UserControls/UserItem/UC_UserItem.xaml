<UserControl x:Name="userControl" x:Class="Axiom.Wpf.Sample.UserControls.UserItem.UC_UserItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Axiom.Wpf.Sample.UserControls.UserItem"
             mc:Ignorable="d">
    <Border CornerRadius="5" Background="#64000000" Margin="0 0 0 10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Label Foreground="White" Content="{Binding UserName, ElementName=userControl}"/>
            <Label Grid.Column="1" Foreground="Gray" Content="{Binding UserSuffix, ElementName=userControl}"/>
        </Grid>
    </Border>
</UserControl>